# 2026-08-17-detail-panel-close-button-removal

## 背景

详情面板头部的 ✕ 关闭按钮与 Esc、再点同行两种取消选中方式功能重复，用户决定移除。

## 目标

- 移除面板头部 ✕ 按钮及其样式（`.panel-close`），头部仅保留专辑名/艺术家（占位态标题不变）
- 保留 Esc 与再点同行两种取消选中方式

## 非目标

- 不改面板头部布局其余部分、不改占位态
- 不动 `closeDetail()`（Esc 取消选中仍使用）
