# 任务清单

## 实现

- [x] `App.vue` 模板：删除面板头部 `<button class="panel-close">✕</button>`
- [x] `App.vue` 样式：删除 `.panel-close` / `.panel-close:hover` 规则
- [x] 更新 `openspec/specs/album-detail-expand/spec.md`（「面板头部与取消选中方式」移除 ✕ 相关描述与 scenario；「取消选中重置风格编辑态」同步更新路径）

## 收尾

- [ ] 用户手动 `npm run dev` QA（头部无 ✕；Esc 与再点同行仍可取消选中）
- [x] 归档 change 到 `openspec/changes/archive/`
