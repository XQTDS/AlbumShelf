# 数据导出/导入

## 背景

数据库（SQLite）仅存储在本地，无法跨设备使用。网易云盘方案验证不可行（仅支持音频文件上传），因此先提供手动导出/导入 JSON 文件的能力，让用户通过任意方式（U盘、网盘、微信等）在设备间传递数据。

## 目标

- 支持将全部数据（专辑、曲目、风格标签及关联）导出为单个 JSON 文件
- 支持从 JSON 文件导入数据，以 netease_album_id 为主键做 upsert 合并
- 提供菜单栏入口

## 非目标

- 不实现自动云同步
- 不处理封面图片文件的导出/导入（仅保留 cover_url，换设备后自动重新缓存）
