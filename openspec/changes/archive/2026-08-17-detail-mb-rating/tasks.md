# 任务清单

## 实现

- [x] `App.vue` 模板：元数据区在「评分人数」前新增「MB 评分」meta-item（⭐ + `toFixed(1)` + `rating-badge`，null 时显示"—"）
- [x] 更新 `openspec/specs/album-detail-expand/spec.md`（内容清单与完整数据展示 scenario 补充 MB 评分）

## 收尾

- [ ] 用户手动 `npm run dev` QA（详情面板元数据区显示 MB 评分；无评分时显示"—"）
- [x] 归档 change 到 `openspec/changes/archive/`
