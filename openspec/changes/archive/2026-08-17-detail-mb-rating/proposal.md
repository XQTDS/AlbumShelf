# 2026-08-17-detail-mb-rating

## 背景

详情面板元数据区展示「评分人数 / 曲目数 / 总时长」，缺少专辑的 MusicBrainz 评分（`mb_rating`，列表已有该列）。用户希望在详情页补充展示。

## 目标

- 元数据区新增「MB 评分」项，位于「评分人数」之前，格式与列表一致（⭐ + 保留 1 位小数，琥珀色徽章样式）
- `mb_rating` 为空时显示"—"

## 非目标

- 不改列表列、不改数据同步/补全逻辑
- 不改元数据区其余项与布局
